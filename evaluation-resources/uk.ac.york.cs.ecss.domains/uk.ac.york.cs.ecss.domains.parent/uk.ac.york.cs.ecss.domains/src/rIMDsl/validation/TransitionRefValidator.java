/**
 *
 * $Id$
 */
package rIMDsl.validation;

import rIMDsl.Event;
import rIMDsl.ResourceLocator;
import rIMDsl.State;
import rIMDsl.TransitionSpec;

/**
 * A sample validator interface for {@link rIMDsl.TransitionRef}.
 * This doesn't really do anything, and it's not a real EMF artifact.
 * It was generated by the org.eclipse.emf.examples.generator.validator plug-in to illustrate how EMF's code generator can be extended.
 * This can be disabled with -vmargs -Dorg.eclipse.emf.examples.generator.validator=false.
 */
public interface TransitionRefValidator {
	boolean validate();

	boolean validateEvent(Event value);
	boolean validateState(State value);
	boolean validateLocator(ResourceLocator value);
	boolean validateName(String value);
	boolean validateSpec(TransitionSpec value);
}
