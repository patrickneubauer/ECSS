/**
 *
 * $Id$
 */
package rIMDsl.validation;

import org.eclipse.emf.common.util.EList;

import rIMDsl.Expression;
import rIMDsl.LinkId;
import rIMDsl.SourceField;
import rIMDsl.Title;
import rIMDsl.UriLink;

/**
 * A sample validator interface for {@link rIMDsl.TransitionSpec}.
 * This doesn't really do anything, and it's not a real EMF artifact.
 * It was generated by the org.eclipse.emf.examples.generator.validator plug-in to illustrate how EMF's code generator can be extended.
 * This can be disabled with -vmargs -Dorg.eclipse.emf.examples.generator.validator=false.
 */
public interface TransitionSpecValidator {
	boolean validate();

	boolean validateEval(Expression value);
	boolean validateUriLinks(EList<UriLink> value);
	boolean validateTitle(Title value);
	boolean validateId(LinkId value);
	boolean validateField(SourceField value);
}
